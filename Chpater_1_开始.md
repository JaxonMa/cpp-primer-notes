# **CHAPTER 1** 开始

## 输入输出流
标准库定义了 4 个 IO 对象：`cin`, `cout`, `cerr`, `clog`。其中，`cerr`用于输出警告和错误信息，`clog`用于输出程序运行时的一般性信息。

1. 输入运算符： **`<<`** <br>
接受两个运算对象，左侧的运算对象必须是一个`ostream`对象，右侧的运算对象是要打印的值。<br>
- 计算结果：左侧运算对象，即用于写入给定值的`ostream`对象

<br>

2. 输出运算符： **`>>`** <br>
接受一个`istream`作为左侧运算对象，接受一个对象作为右侧运算对象。
- 计算结果：左侧运算对象，例如：
    ~~~ cpp
    std::cin >> val_1 >> val_2;
    ~~~
    等价于：
    ~~~ cpp
    (std::cin >> val_1) >> val_2;
    ~~~

<br>

3. **注意**<br>
`std::cin`本身可以作为一个条件用于`if`、`while`等控制流语句的判断条件。当遇到文件结尾（EOF）或者是无效的输入（不符合输入要求，例如试图将字符赋给int型变量）时，`istream`对象的状态会变为无效，此时它会使条件变为`false`

## 引用头文件
- 对于来自标准库的头文件，使用尖括号（`< >`）
- 对于不属于标准库的头文件，使用双引号（`" "`）

## 使用文件重定向
大多数操作系统支持文件重定向，这种机制允许我们将标准输入与标准输出与命名文件关联起来：
~~~ bash
$ addItem <infile >outfile
~~~
其中，`addItem`是已经编译好的加法程序。上述命令会读取`infile`中的内容并传入，然后将输出结果写入到名为`outfile`的文件中。

## 作用域运算符与点运算符
- 作用域运算符（`::` ）的左值在编译时就确定，常见用法有：
  - 全局作用域访问：用`::variable`可以直接访问全局变量，不受重名的局部变量影响
  - 类作用域访问：用于在类外部实现成员函数，或初始化类的静态成员（例如`int Dog::num = 0;`
  - 命名空间作用域访问：访问某个命名空间内的成员，例如`std::cout`
- 点运算符（`.`）
  - 左侧运算对象必须是一个类类型的对象（用`class`, `struct`定义的都是），右侧运算对象必须是该类型的一个成员名
  - 运算结果：右侧运算对象指定的成员

## 杂项
- 注释不能嵌套