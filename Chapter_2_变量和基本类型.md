# **CHAPTER 2** 变量和基本类型

## 内置类型的机器实现
| 术语 | 解释 |
| --- | --- |
| 字节（byte）| 可寻址的最小内存块。 |
| 字（word） | 存储的基本单元，通常由几个字节构成 |

大多数计算机都有：1 字节 = 8 位（bit），字通常由 32 或 64 比特构成，也就是 4 字节或 8 字节，也就是我们熟悉的“32-bit”“64-bit”。

大多数计算机将内存中的 **每个字节** 与 **一个数字** 关联起来。这个数字就是 **地址（address）**。例如在一台满足“1 byte = 8 bit, 32-bit”的机器上，有四个地址指向它的一个字（32-bit 意味着它的一个字由 4 字节构成）。

<br>

> **注意**: 任意类型的指针大小都等于平台的字的大小. 例如 32 位系统下的指针全部占 4 字节, 64 位系统下的指针全部占 8 字节. 这是因为指针要能够存储一个完整的内存地址.

> 一个 `char` 的大小始终和一个机器字节一样，`sizeof(char) = 1` 恒成立。

## 基本内置类型
- 包含 **算术类型** 和 **空类型** 等。
- `bool`, `char`, `short` 等都是算术类型
- `unsigned` 其实就是 `unsigned int`

1. 算术类型的尺寸
   
   不同算术类型的尺寸在不同机器上有所差别，但 C++ 标准规定了每个类型的最小尺寸。注意最小尺寸不等于常用尺寸，例如：`int` 的最小尺寸是 16 bit，但大部分机器上都是 32 bit.

   | 类型 | 含义 | *最小尺寸* |
   | --- | --- | --- |
   | `bool` | 布尔类型 | 未定义 |
   | `char` | 字符 | 8 bit |
   | `wchar_t` | 宽字符 | 16 bit |
   | `char16_t` | **Unicode** 字符 | 16 bit |
   | `char32_t` | **Unicode** 字符 | 32 bit |
   
   以上本质上都是 **算术类型**，它们都是 **整型**，只不过通常我们不会用它们来存储字面量的数字。以下是常用来存储数字的类型：

   | 类型 | 含义 | *最小尺寸* |
   | --- | --- | --- |
   | `short` | 短整型 | 16 bit |
   | `int` | 整型 | 16 bit |
   | `long` | 长整型 | 32 bit |
   | `long long` | 长整型 | 64 bit |
   | `float` | 单精度浮点型 | 6 位 **有效数字**（IEEE 754 标准下通常约 **7 位**） |
   | `double` | 双精度浮点型 | 10 位 **有效数字**（IEEE 754 标准下通常约 **15~16 位**）|
   | `long double` | 扩展精度浮点型 | 10 位 **有效数字** |

   **注意**： C++ 语言规定，一个 `int` 至少和一个 `short` 一样大，一个 `long` 至少和一个 `int` 一样大，一个 `long long` 至少和一个 `long` 一样大。

<br>

2. 如何选择类型
   - ~~明知数值不可能为负时，使用无符号（`unsigned`）类型~~
     > 这是 C++ Primer 旧版的观点. 现代 C++ 核心指南 (C++ Core Guidelines) 明确建议 "不要使用无符号类型表示数量", 因为无符号数的 "回绕 (underflow)" 极易引发隐蔽 bug. 例如:
     ~~~ cpp
     for (unsigned i = 10; i >= 0; i--);
     ~~~ 
     > 会导致死循环. 因为 `i` 最后会回绕到 `UINT_MAX - 1` (注意不是 `INT_MAX`, 有符号数和无符号数的最大值不一样).
   - 使用 `int` 做整数运算
     - 不建议使用 `short`，因为太小
     - 如果数字太大，直接使用 `long long`。因为 `long long` 严格保证大小至少为 64 bit，而 `long` 一般和 `int` 有一样的大小
   - 使用 `double` 做浮点数运算
     - `float` 通常精度不够
     - `float` 与 `double` 的计算代价相差无几
     - 一般不需要使用 `long double`，而且它带来的运行时消耗不容忽视
   - 不要用 `char` 或者 `bool` 做数学运算

<br>

1. 类型转换
   - 布尔类型
     - 布尔类型 → 非布尔类型<br>
        真 1 假 0（初始值为 `true` 则结果为 1，初始值为 `false` 则结果为 0）
     - 非布尔类型 → 布尔类型<br>
        为 0 才假（初始值为 0 则结果为 `false`，否则为 `true`）
   - 浮点型与整数
     - 整数值 → 浮点类型<br>
        小数部分记为 0。如果超出浮点型容量，精度可能有损失
     - 浮点类型 → 整数值<br>
        仅保留小数点之前的部分
   - 无符号类型
     - 若超出范围，结果是初始值对无符号类型表示数值总数取模后的余数（值会“循环”，例如，将 -1 赋给 8 比特大小的 `unsigned char` 所得到的结果是 255）
   - 带符号类型
     - 若超出范围，结果是 **未定义的（undefined）**，不得出现这样的行为 

2. 含有无符号类型的表达式<br>
    无符号数永远不可能小于 0。当有符号类型与无符号类型相互进行计算时，会**先将有符号数先转为无符号数**，再进行计算。

    **注意**：切勿混用带符号类型和无符号类型。当带符号类型为负时会出现异常结果。
    
    **注意**：“混用”不止包括数学计算，还包括条件判断，例如：
    ~~~ cpp
    unsigned num1 = 1;
    int num2 = -1;

    if (num1 < num2) {
      // 后续代码
    }
    ~~~
    几乎可以肯定，`if` 的条件将会始终为 `true`，因为判断前先将 `num2` 转换成无符号整型，结果将会是一个非常大的数。

<br>

## 字面值常量
1. 指定字面值的类型
   - 以 0 开头的整数代表 *八进制数*
   - 以 0x 开头的整数代表 *十六进制数*
   - 若 `\` 后跟着的八进制数字超过 3 个，则只有前 3 个数字与 `\` 构成转义序列。
   - 十六进制转义字符 `\x` 后跟着的所以数字都会与其构成转义序列
  
   通过添加前缀和后缀可改变字面值的默认类型，注意 **可以组合使用**：
   
   对于 *字符和字符串字面值*，有：
   | 前缀 | 含义 | 类型 |
   | --- | --- | --- |
   | `u` | **Unicode** 字符 | `char16_t` |
   | `U` | **Unicode** 字符 | `char32_t` | 
   | `L` | 宽字符 | `wchar_t` |
   | `u8` | **UTF-8**（仅用于字符串字面常量） | `char` |

   <br>

   对于 *整型字面值*，有：
   | 后缀 | *最小匹配类型* |
   | --- | --- |
   | `u` or `U` | `unsigned` |
   | `l` or `L` | `long` |
   | `ll` or `LL` | `long long` |

   <br>

   对于 *浮点型字面值*，有：
   | 后缀 | 类型 |
   | --- | --- |
   | `f` or `F` | `float` |
   | `l` or `L` | **`long double`** |

2. 注意事项
   - 浮点型字面值的默认类型为 `double`
   - 对于一个字符串字面值，无论是否显式写出 `\0`，编译器总会隐式在末尾加一个 `\0`。在获取字符串长度时，`strlen()` 是“遇 `\0` 即止”，而 `sizeof()` 直到最后一个 `\0` 才会停止，其获得的字符串长度通常会比可见的字符数目多 1。 

<br>

## 变量与对象
通常情况下，对象是指一块能存储数据并具有某种类型的内存空间。

1. 初始化与赋值

   | 术语 | 解释 |
   | --- | --- |
   | 初始化 | 创建变量时赋予其一个初始值 |
   | 赋值 | 将对象的当前值擦除，并用一个新值来替代 |

   **注意**：在 C++ 中，初始化不等于赋值，这是两个完全不同的操作，只不过在很多编程语言中两者的区别几乎可以忽略不计。

   初始化有以下几种形式：
   ~~~ cpp
   int units_sold = 0;  // 最常用的
   int units_sold = {0};  // 列表初始化
   int units_sold{0};  // 列表初始化
   int units_sold(0);  // 我没见过，但不是列表初始化
   ~~~
   使用列表初始化的优点：若存在丢失精度的危险，则编译器将报错。

<br>

2. 默认初始化

   对于内置类型的变量，如果在任何函数体之外，则其被初始化为 0；如果在函数体内部，则其不会被初始化（uninitialized），其值是未定义的（undefined）。

   每个类各自决定其初始化对象的方式。而且，是否允许不初始化就定义对象也由类自己决定。如果类允许这种行为，它将决定对象的初始值到底是什么。

   建议初始化每一个内置类型的变量。

<br>

3. 声明与定义

   | 术语 | 解释 |
   | --- | --- |
   | 声明 | 使用 `extern` 关键字，使名字为程序所知 |
   | 定义 | 创建与名字相关的实体 |

   > **定义一定是声明，声明不一定是定义。**
   >
   > 变量能且只能被定义一次，但是可以多次声明

   **注意**：`extern` 不会更改变量的作用域。C++ 标准不支持在函数体内部用 `extern` 声明变量，绝大多数 C++编译器会直接报错。即便在函数体内部使用 `extern` 定义一个变量，它的作用域仍然是“定义语句开始到函数结束”。只不过在理论上可以被跨文件访问。

   **注意**：`extern` 的效果 *不是* 使变量能从外部访问，而是实现跨文件共享同一个全局变量，这样就避免了重复定义，占用不必要的内存空间。

   <br>

   使用 `extern` 关键字且不显式初始化变量，就完成了一次声明。这告诉编译器“该变量在别处定义了，不用为其分配内存空间”，编译器就会放行，并在后续使用时链接。但如果对变量初始化，则会强制为其分配内存空间，这就抵消了 `extern` 的效果。
   ~~~ cpp
   extern int i; // 声明
   int j;  // 声明 + 定义（因为这里为 j 分配了内存空间）

   // 声明 + 定义（尽管使用了 extern，但赋值抵消了它 “不分配内存，仅做外部声明” 的作用）
   extern double pi = 3.14;
   double pi = 3.14;
   // 从外部访问性来看，这两行代码几乎无异。再次强调：extern不是使变量能够从外部访问
   ~~~

<br>

## 复合类型的两种——引用与指针
1. 引用
   
   > 严格来说，我们此处说的“引用”，其实指的是“左值引用”。另一种”右值引用“将会在13.6.1节做更详细的介绍。

   引用并非对象，而只是为已经存在的对象起了一个别名。引用 **必须初始化**，一旦初始化完成，引用将和它的初始值对象一直绑定在一起。不能直接绑定字面量/临时值。例如：

   ~~~ cpp
   int &ref = 0;  // 错误
   ~~~

   从效果上来看，引用很像是常量指针，但是不能完全等同：
   - `sizeof(ref)` 返回的是被引用对象的大小，而 `sizeof(ptr)` 返回的是指针自身的大小
   - `&ref` 获得的是被引用对象的地址，而 `&ptr` 返回的是指针自身的地址
   - 引用不能为 `null`，但常量指针可以为 `nullptr`
   - 引用不支持指针算数

   所有引用的类型都要和与之绑定的对象严格匹配，除了两种例外情况：
   - 指向常量的引用（“常量引用”）
     - 初始化常量引用时允许使用任何值，只要表达式的结果可以转换为引用的类型即可（例如 3.14 可以被转换为 3，从而初始化一个 `int &` 的引用）。
     - > 一般来说，使用引用是为了更改原对象的值。但若将非常量引用绑定到一个与它类型不同的数据上，那么就只能将引用绑定到“编译器生成的临时量”，而非原对象上，如此就不能更改原对象的值，那么就与初衷相悖，C++ 语言便将这种行为定义为非法。
   - （将在15.2.3节介绍）

<br>

2. 指针
   
   > **注意**: 任意类型的指针大小都等于平台的字的大小. 例如 32 位系统下的指针全部占 4 字节, 64 位系统下的指针全部占 8 字节. 这是因为指针要能够存储一个完整的内存地址.

   指针本身就是一个对象，允许对指针进行赋值和拷贝。在指针的生命周期内，它可以先后指向几个不同的对象。

   所有指针的类型都要和它指向的对象严格匹配，除了两种例外情况：
   - 指向常量的指针可以指向一个非常量对象
   - （将在15.2.3节介绍）
   
   指针的值应当满足下列 3 种状态之一，否则就为无效指针：
   - 指向一个对象
   - 指向紧邻对象所占空间的下一个的位置
   - 空指针（没有指向任何对象）
   
   试图拷贝或以其他方式访问无效指针的值都会引发错误，而编译器并不负责检查此类错误，这一点和试图使用未经初始化的变量是一样的。一定要避免访问无效指针。

   在使用指针作为条件时，任意非空指针的条件值都为 `true`

   <br>

   ### 空指针
   生成空指针的方式有：
   ~~~ cpp
   int *ptr = nullptr;
   int *ptr = 0;
   ~~~
   或使用 C 的风格：
   ~~~ c
   #include <cstdlib>
   int *ptr = NULL;
   ~~~

   注意不能将 `int` 变量赋值给指针，即便其值为 0。此外，建议初始化所有指针。

   <br>

   ### `void*` 指针

   `void*` 指针可用于存放任意对象的地址。这也是为什么使用 C 语言的 `malloc()` 后可以直接转为其他类型。

   不能直接操作 `void*` 指针所指的对象，因为我们并不知道这个对象到底是什么类型，也就无法确定在该对象上能够进行哪些操作。

<br>

3. 指向指针的引用
   
   引用本身不是一个对象，因此不能定义指向引用的指针，但反过来，可以定义对指针的引用：
   ~~~ cpp
   int i = 42;
   int *ptr = &i;
   int *&ref = ptr;  // 对指针ptr的引用。注意 “类型要相同”
   ~~~
   阅读时建议 **从右往左阅读**，因为离变量名最近的符号对其类型有最直接的影响。

<br>

4. 注意
   
   - 给定指针 p，不能判断它是否指向了一个合法的对象。我们只能够可以判断它是否为空。
   - 只有字面量 0、编译期可得的 `const` 量、`nullptr` 可以被赋值给空指针

<br>

## `const` 限定符
`const` 修饰左边紧邻的东西，若没有才修饰右边的。

`const` 一旦创建就不可修改，因此必须初始化，初始值可以是任意复杂的表达式。可以使用非 `const` 对象去初始化 `const` 对象。

默认状态下，`const` 对象仅在文件内有效。在不同文件中定义同名的常量，实际上是分别定义了独立的变量。如果要实现跨文件共用一个常量，那么可以使用 `extern` 关键字。这样，只会对一个常量分配内存空间，而其他文件就可以直接使用该常量。

> 指向 `const` 的指针/引用不一定会指向/引用一个常量。这个名字只是在说：“它们觉得自己指向/引用了一个常量，那么就不能通过它们去更改对应的值。”有点一厢情愿的感觉。

<br>

### `const` 与引用
常量引用是“对 `const` 的引用”的简称。要注意区分：“常量指针”与”指向 `const` 的指针“是两个概念，而”常量引用”与“对 `const` 的引用”指的是同一个东西，只不过前者是对后者的简称。注意**引用不是对象**，因此严格意义上来说，**不存在“本身就是常量的引用”**，没有“常量引用”这个说法。

常量引用允许使用任意类型的表达式作为初始值，只要该表达式的结果能够转换成引用的类型即可。在这种情况下，编译器实际上是创建了一个临时量对象，并让引用绑定这个临时量。由于被引用的对象与引用本身都是 `const`，也就不存在在运行时更改值的需求，因此这样做没有问题。**包括字面量**，例如：

~~~ cpp
const int &ref = 0;  // 这样做合法
~~~

非常量引用不能指向常量对象：常量是不可变的，不能通过引用去改变它的值。指向常量对象的引用必须是常量引用——这其实是“引用的类型必须与被引用对象严格一致”的体现。

但是要注意：**常量引用可以引用一个非常量对象**，但是**不能通过常量引用更改被引用对象的值**，这与下文“指向 `const` 的指针”是一脉相通的。例如：
~~~ cpp
int i = 42;

// 两个引用都合法
int &ref_i = i;
const int &REF_I = i;

ref_i = 0;  // 合法行为
REF_I = 0;  // 错误：不可以通过 REF_I 更改其所绑定对象的值
~~~

指向常量的引用（“常量引用”）中，`const` 都是底层 `const`，因为就如上文所述，引用的表现就像一个“常量指针”。这里的 `const` 修饰的其实是被绑定的对象。

<br>

### `const` 与指针
注意：“指向 `const` 的指针”与“常量指针（`const` 指针）”是两个不同的概念。

#### 指向 `const` 的指针
要想存放常量对象的地址，只能使用指向 `const` 的指针。指向 `const` 的指针不能用于改变其所指对象的值。注意 **这并不代表指针本身就是常量**。

指向常量的指针 **不代表指针本身是常量**，而是指不能通过该指针去更改其所指对象的值，因此初始化时也完全可以使其指向一个非常量对象。在书写时很容易出现误解：因为指针的类型必须和它指向的对象一致，因此指向常量的指针在书写时一定会带上一个 `const`，所以要注意：

~~~ cpp
const int val = 42;
const int *ptr = &val;  // 这不代表 ptr 就是常量
~~~

此外，允许指向常量的指针指向一个非常量对象，即便是在初始化的时候：
~~~ cpp
const int num = 9;
const int *ptr = &num;

double pi = 3.14;
ptr = &pi;  // 通过赋值改变，这样做是合法的
const double *ptr2 = &pi;  // 这样做也是合法的
~~~

<br>

#### `const` 指针（常量指针）
指针是对象，而引用不是，因此可以将指针本身定义为常量，这样的指针就是常量指针。常量指针必须初始化。建议用 `*const ptr` 的格式书写，暗含“不变的是指针的值”的意味。

<br>

#### 指向常量的常量指针
看下面这个例子：
~~~cpp
const int val = 1;
const int *const ptr = &val;  // 靠右的 const 是顶层 const，靠左的 const 是底层 const
~~~

出现了两个 `const`，这两个 `const` 的类型不同：
| 术语 | 解释 |
| --- | --- |
| 顶层 `const` (top-level const) | 表示指针本身就是一个常量 |
| 底层 `const` (low-level const) | 表示指针所指的对象是一个常量 |

底层 `const` 只会出现在复合类型中。目前所学的内容中，只有指针会出现这样有两个 `const` 的情况。重点在于：是“指针本身是一个常量”还是“指针指向的对象是一个常量”，只在这个范围内讨论。

当执行拷贝操作时，顶层 `const` 不会造成影响，但对于底层 `const`，拷入和拷出的对象必须具有相同的底层 `const` 资格，或者两个对象的数据类型能够转换，一般来说，非常量可以转换为常量，反之则不行。

<br>

### `constexpr` 变量
- 所有 `constexpr` 变量天然具有 `const` 属性
- 普通 `const` 的初始化可以推迟到运行时，而 `constexpr` 变量必须在编译器就完成初始化和值计算。例如：
  ~~~ cpp
  int i = 0;
  const int j = i;  // 合法
  constexpr int j = i;  // 非法
  ~~~
- 指针和引用都能定义成 `constexpr`，但初始值受到严格限制。一个 `constexpr` 指针的初始值必须是 `nullptr` 或 0，或者是存储与某个固定地址中的对象。
- `constexpr` 只对指针有效，与指针所指的对象无关。也可理解成，`constexpr` 是一个恒定的顶层 `const`

<br>

## 类型别名
- 使用关键字 `typedef`，注意处理复合类型时容易造成误解，建议用`*`, `&`靠近类型名的写法
  
  ~~~ cpp
  typedef double new_name
  ~~~
- 使用别名声明（关键字 `using`）
  ~~~ cpp
  using new_name = double;
  ~~~

<br>

## `auto` 类型说明符
- 必须有初始值
- 一条语句声明的多个变量中，所有变量的初始数据类型必须一致
- `auto` 一般会忽略 **顶层 `const`** 而保留 **底层 `const`**。如果希望推断出的 `auto` 类型是一个顶层 `const`，那么需要明确指出：
  ~~~ cpp
  const int ci = 0;

  auto ptr = ci;  // ptr 是 int *
  auto a_ptr = &ci;  // 对常量对象取地址是一种底层 const，a_ptr 是 const int *
  const auto ca_ptr = &ci;  // ca_ptr 是 const int *const
  ~~~
- 可以将引用的类型设为 `auto`，此时原来的初始化规则仍然适用。例如：
  ~~~ cpp
  const int ci = 0;
  auto &ref = ci;  // 合法
  
  auto &a_ref = 0;  // 非法
  const auto &ca_ref = 42;  // 合法
  ~~~
- 对指针的补充（对符号 `*` 的补充）
  ~~~ cpp
  int val = 10;

  // 以下两行代码完全等价
  auto p1 = &val;
  auto *p2 = &val;
  ~~~

<br>

## `decltype` 类型指示符
如果希望从表达式的类型推断出要定义的变量的类型，但是又不希望用该表达式的值初始化变量，那么这时候可以使用 `decltype` 类型指示符。编译器分析表达式并得到它的类型，却并不实际返回表达式的值。注意：在使用时要遵循相应类型的初始化规则。

- 如果表达式的内容是一个变量，那么 `decltype` 会返回该变量的类型，**包括顶层 `const` 和底层 `const`**。例如：
  ~~~ cpp
  const int val = 0， &ref = val;
  decltype(val) num = 9;  // num 的类型是 const int
  decltype(ref) ref_num = num;  // ref_num 的值是 const int &
  ~~~
- 如果表达式的内容是一个变量，而变量名 **加上了括号**，那么始终会得到 **引用类型**
  ~~~ cpp
  int num = 0;
  decltype((num)) val = num;  // val 是一个引用，与 num 绑定
  decltype(num) var;  // var 是一个未初始化的 int
  ~~~
- 如果表达式的内容是 **解引用操作**，那么 `decltype` 会得到 **引用类型**。例如：
  ~~~ cpp
  int num = 0;
  int *ptr = &num;
  decltype(*ptr) val = num;  // val 的类型是 int &
  ~~~

> `auto` 会忽略顶层 const，而 `decltype` 不会。
> 
> 在只有底层 const，或无 const 的时候，可以使 `auto` 与 `decltype` 指定的类型相同。

<br>

## 自定义数据结构——结构体
结构体本质是类的特殊形式，关于类的介绍将在后续章节展开。结构体使用关键字 `struct` 定义，其语法如下，注意结尾有分号：
~~~ cpp
struct OriginalCharacter { /* ... */ };
OriginalCharacter my_oc;

// 也可以这么写，但是不推荐
struct OriginalCharacter { /* ... */ } my_oc;
~~~
定义类 **数据成员** 的方法和定义普通变量一样。我们可以为数据成员提供一个 **类内初始值**。

使用点运算符可以访问结构体内的数据。

<br>

## 编写自己的头文件
类通常定义在头文件中，且类所在头文件的名字应当与类的名字一样。头文件一旦改变，相关的源文件必须重新编译以获取更新过的声明。

### 预处理器概述
头文件可能会被多次包含，这样的话可能引发异常。为确保头文件多次包含仍能安全工作的常用技术是 **预处理器（preprocessor）**

如下，使用 **头文件保护符（header guard）** 防止重复包含
~~~ cpp
#ifndef ORIGINAL_CHARACTER_H
#define ORIGINAL_CHARACTER_H
struct OriginalCharacter { /* ... */ };
#endif
~~~
> 翻译：如果 `ORIGINAL_CHARACTER_H` 这个 **预处理变量** 没有被定义，那么定义其，同时声明结构体，最后遇到 `#endif` 停止。

在重复包含时，`#ifndef` 不成立，直接跳过，这样就避免了重复包含带来的问题。建议在任何头文件中都加上头文件保护符。

**注意**: 预处理变量无视 C++ 中关于作用域的规则。